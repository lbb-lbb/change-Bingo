import Vue from 'vue'

import {
  Button,
  Menu,
  MenuItem,
  Submenu,
  Container,
  Header,
  Main,
  Footer,
  Avatar,
  Pagination,
  Col,
  Tag,
  Autocomplete,
  Icon,
  Card,
  Row,
  Form,
  FormItem,
  Input,
  Divider
} from 'element-ui'

Vue.use(Button)
  .use(Menu)
  .use(MenuItem)
  .use(Submenu)
  .use(Container)
  .use(Header)
  .use(Main)
  .use(Footer)
  .use(Avatar)
  .use(Pagination)
  .use(Col)
  .use(Tag)
  .use(Autocomplete)
  .use(Icon)
  .use(Card)
  .use(Row)
  .use(Form)
  .use(FormItem)
  .use(Input)
  .use(Divider)
